Put your two personal files in this folder, using these exact names
(or update the paths in index.html if you rename them):

1. profile.jpg   -> your profile picture (jpg or png; if png, also change
                    the file extension in index.html's <img src="..."> tag)

2. cv.pdf        -> your CV/resume PDF, exported from LaTeX
                    (this also drives the inline preview in the CV section —
                    no extra setup needed, it just reads the same file)

That's it — index.html already links to "assets/profile.jpg" and
"assets/cv.pdf", so once these two files are here, your site is complete.
